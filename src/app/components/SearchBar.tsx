import React from "react";

const SearchBar = ({ toSearch }: { toSearch: (res: string) => void }) => {
  return (
    <div>
      <input type="text" name="" id="" />
    </div>
  );
};

export default SearchBar;
